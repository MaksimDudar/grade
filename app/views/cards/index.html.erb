<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <p style="color: green"><%= notice %></p>

<h3>CardiTrans (<%= send(:range2) %>)</h3>
<p> <%= link_to "Новый отзыв", new_card_path %> </p><hr>

<div id="cards">
  <% @cards.each do |card| %>
    <% render card %>
    <p>
      <%= link_to "Отзыв № #{card.id}", card %>
    </p>
  <% end %>
</div>
    </div>
  </div>
</div>
